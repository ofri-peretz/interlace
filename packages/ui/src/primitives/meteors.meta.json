{
  "$comment": "Animation tokens belong in cssVars.theme, NOT in css['@theme inline'] — the shadcn CLI's update-css step fails to parse the latter (`Unknown word meteor`), which aborted the whole install. Caught by apps/registry/scripts/e2e-install.mjs; same convention magicui's own registry uses.",
  "cssVars": {
    "theme": {
      "animate-meteor": "meteor 5s linear infinite"
    },
    "light": {
      "meteor-glow": "oklch(1 0 0 / 0.06)"
    },
    "dark": {
      "meteor-glow": "oklch(1 0 0 / 0.1)"
    }
  },
  "css": {
    "@keyframes meteor": {
      "0%": {
        "transform": "rotate(215deg) translateX(0)",
        "opacity": "1"
      },
      "70%": {
        "opacity": "1"
      },
      "100%": {
        "transform": "rotate(215deg) translateX(-500px)",
        "opacity": "0"
      }
    },
    "@media (prefers-reduced-motion: reduce)": {
      ".animate-meteor": {
        "animation": "none !important"
      }
    }
  }
}
