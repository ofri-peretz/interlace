/**
 * PostCSS configuration — Tailwind v4 via the dedicated postcss plugin.
 * Same pattern as apps/docs.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
