{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npx turbo run build --filter=registry",
  "installCommand": "cd ../.. && npm install --include=dev --no-audit --no-fund",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
