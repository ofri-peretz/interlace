{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "popover",
  "type": "registry:ui",
  "title": "Popover",
  "description": "@interlace/ui — popover (shadcn-compatible).",
  "author": "ofri-peretz <https://github.com/ofri-peretz>",
  "categories": [
    "overlay",
    "primitive"
  ],
  "dependencies": [
    "@base-ui/react"
  ],
  "registryDependencies": [
    "https://ds.interlace.tools/r/theme.json"
  ],
  "files": [
    {
      "path": "registry/interlace-ui/popover.tsx",
      "target": "components/ui/popover.tsx",
      "type": "registry:ui",
      "content": "'use client';\n// @interlace/popover v1.2.0 — Interlace design system.\n// Docs, props and live preview: https://ds.interlace.tools/c/popover\n// What changed since: https://ds.interlace.tools/c/popover#history\n// Generated banner — keep it, the upgrade diff reads this version.\n\nimport * as React from 'react';\nimport { Popover as BasePopover } from '@base-ui/react/popover';\n\nimport { cn } from '@/lib/utils';\n\n/**\n * Minimum viable viewport (CSS px) — DESIGN_PRINCIPLES #14. The popup is\n * A NON-MODAL floating panel: dismisses on Escape and on an outside click, and\n * restores focus to the trigger on close, but does NOT trap focus or make the\n * page inert — that is the line between this and Dialog/Sheet, and it is the\n * reason to pick one over the other. Base UI owns the behaviour; naming it here\n * is what makes the component findable by what it does.\n * * `w-72` (288px), which still clears the 320 floor with gutters; Base UI's\n * positioner flips/shifts it into the viewport rather than letting it clip.\n * Projected onto the popup — `Popover.Root` renders no DOM node.\n */\nexport const MIN_VIEWPORT = 320 as const;\n\nfunction Popover(props: React.ComponentProps<typeof BasePopover.Root>) {\n  return <BasePopover.Root data-slot=\"popover\" {...props} />;\n}\n\nfunction PopoverTrigger(\n  props: React.ComponentProps<typeof BasePopover.Trigger>,\n) {\n  return <BasePopover.Trigger data-slot=\"popover-trigger\" {...props} />;\n}\n\nfunction PopoverAnchor(\n  props: React.ComponentProps<typeof BasePopover.Trigger>,\n) {\n  return <BasePopover.Trigger data-slot=\"popover-anchor\" {...props} />;\n}\n\nfunction PopoverContent({\n  className,\n  side,\n  align = 'center',\n  sideOffset = 4,\n  children,\n  ...props\n}: React.ComponentProps<typeof BasePopover.Popup> & {\n  side?: 'top' | 'right' | 'bottom' | 'left' | 'inline-start' | 'inline-end';\n  align?: 'start' | 'center' | 'end';\n  sideOffset?: number;\n}) {\n  return (\n    <BasePopover.Portal>\n      <BasePopover.Positioner side={side} sideOffset={sideOffset} align={align}>\n        <BasePopover.Popup\n          data-slot=\"popover-content\"\n          data-min-viewport={String(MIN_VIEWPORT)}\n          className={cn(\n            'bg-popover text-popover-foreground data-[open]:animate-in data-[closed]:animate-out data-[closed]:fade-out-0 data-[open]:fade-in-0 data-[closed]:zoom-out-95 data-[open]:zoom-in-95 z-50 w-72 origin-(--transform-origin) rounded-md border p-4 shadow-md outline-hidden',\n            className,\n          )}\n          {...props}\n        >\n          {children}\n        </BasePopover.Popup>\n      </BasePopover.Positioner>\n    </BasePopover.Portal>\n  );\n}\n\n/* ─────────────────────────────────────────────────────────────────\n * PopoverCompose — convenience composition.\n *\n * Wraps Root + Trigger + Content (which already bundles\n * Portal + Positioner + Popup) in a single component for the common case.\n *\n *   <PopoverCompose\n *     trigger={<Button variant=\"ghost\">Filters</Button>}\n *     content={<FilterPanel />}\n *     side=\"bottom\"\n *     align=\"start\"\n *   />\n * ──────────────────────────────────────────────────────────────── */\ninterface PopoverComposeProps {\n  trigger: React.ReactNode;\n  content: React.ReactNode;\n  open?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** Floating-UI side. */\n  side?: React.ComponentProps<typeof PopoverContent>['side'];\n  /** Floating-UI alignment along the side. */\n  align?: React.ComponentProps<typeof PopoverContent>['align'];\n  className?: string;\n}\n\nfunction PopoverCompose({\n  trigger,\n  content,\n  open,\n  onOpenChange,\n  side,\n  align,\n  className,\n}: PopoverComposeProps) {\n  return (\n    <Popover open={open} onOpenChange={onOpenChange}>\n      <PopoverTrigger render={trigger as React.ReactElement} />\n      <PopoverContent side={side} align={align} className={className}>\n        {content}\n      </PopoverContent>\n    </Popover>\n  );\n}\n\n// Dotted access — `<Popover.Compose ...>`. See dialog.tsx for pattern.\nconst PopoverWithDot = Object.assign(Popover, {\n  Compose: PopoverCompose,\n}) as typeof Popover & { Compose: typeof PopoverCompose };\n\nexport {\n  PopoverWithDot as Popover,\n  PopoverTrigger,\n  PopoverContent,\n  PopoverAnchor,\n  PopoverCompose,\n};\nexport type { PopoverComposeProps };\n"
    }
  ],
  "meta": {
    "tier": "primitive",
    "client": true,
    "minViewport": 320,
    "loading": false,
    "version": "1.2.0",
    "since": "1.0.0"
  },
  "docs": "## @interlace/popover\n\nInstalled to `components/ui/popover.tsx`.\n\n```tsx\nimport { /* … */ } from '@/components/ui/popover';\n```\n\nProps, a11y contract, live preview and source: https://ds.interlace.tools/c/popover\n\nRequires the `@interlace/theme` CSS baseline (installed automatically as a registry dependency)."
}
